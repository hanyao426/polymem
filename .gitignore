# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node / Bun (transitional — will be removed when we port to all-Python)
node_modules/
bun.lock
package-lock.json
yarn.lock

# Build artifacts
dist/
build/
*.whl
*.tar.gz

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*.swo

# Engine runtime data (lives in ~/.polymem, not in repo, but exclude defensively)
*.db
*.db-shm
*.db-wal
chroma/
engine.log

# Backups generated by install scripts
*.bak.*

# Local config overrides
.env
.env.local
